galaxy_info:
  author: jedimt
  role_name: kube_prometheus
  description: Install kube-prometheus project with persistent volumes

  license: MIT

  min_ansible_version: "5"

  platforms:
    - name: Ubuntu
      versions:
        - focal
        - xenial
        - jammy

  galaxy_tags:
    - prometheus
    - kubeprometheus
    - kubernetes
    - monitoring

dependencies:
  - role: jedimt.golang_install
